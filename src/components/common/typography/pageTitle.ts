import theme from "@/src/common/styles/theme";
import styled from "@emotion/styled";

export const PageTitle = styled.h2`
  font-size: ${theme.fontSize.fs80};
  color: ${theme.colors.main.primary};
`;
